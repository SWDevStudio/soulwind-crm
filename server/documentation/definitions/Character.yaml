definitions:
  CharacterDTO:
    type: "object"
    required:
      - "password"
    properties:
      email:
        type: "string"
        format: "email"
      roles:
        type: "string"
        enum:
          - "USER"
          - "ADMIN"
      password:
        type: "string"
    xml:
      name: "UserDTO"
